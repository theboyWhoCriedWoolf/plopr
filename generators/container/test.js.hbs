// import React from 'react';
// import { mount } from 'enzyme';
// import renderer from 'react-test-renderer';
// import { enzymeFind } from 'styled-components/test-utils';
// import 'jest-styled-components';

import {{ properCase name }} from '..';

describe('< {{ properCase name }} />', () => {
    
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });

    // it('should render an <typeOfElement> tag', () => {
        // const wrapper = mount(< {{ properCase name }} />);
        // const renderedComponent = enzymeFind(wrapper, {{ properCase name }});
        // expect(renderedComponent.type()).toEqual('typeOfElement');
    // });

    // it('should match snapshot', () => {
        // const tree = renderer.create(< {{ properCase name }} />).toJSON();
        // expect(tree).toMatchSnapshot();
    // });
});